---
name: project-init
description: "REDIRECT: This skill has been merged into /project init. Run /project init instead."
---

# Project Init — Redirected

This skill has been merged into `/project init`.

Run `/project init` instead — it does everything this skill did (filing cabinet, SPEC.md, tasks) plus lifecycle setup (phases, board touchpoints, verification).

If you're seeing this, update your routing to use `/project init`.

**Note:** `/init` is a Claude Code built-in command that generates CLAUDE.md. Use `/project init` (with space) for lifecycle setup — they are different commands.
